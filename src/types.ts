export interface InitSettings {
  genesisFirstName: string;
  genesisLastName: string;
  genesisEmail: string;
  genesisPassword: string;
}

export interface MiningSettings {
  minerParticipantId?: string;
}
